{
  "name": "electron-builder-host",
  "version": "$(Version)",
  "private": true,
  "build": {
    "appId": "$(ElectronPackageId)",
    "win": {
      "executableName": "$(ElectronExecutableName)"
    },
    "mac": {
      "executableName": "$(ElectronExecutableName)"
    },
    "linux": {
      "desktop": {
        "entry": { "Name": "$(Title)" }
      },
      "executableName": "$(ElectronExecutableName)"
    },
    "deb": {
      "desktop": {
        "entry": { "Name": "$(Title)" }
      }
    }
  }
}
